# /etc/nginx/nginx.conf

user nginx;
worker_processes  auto;
pid /run/nginx.pid;

events { worker_connections 1024; }

http {
  # 基本优化
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;
  sendfile on;
  tcp_nopush on;
  keepalive_timeout 65;

  # 登录/下载口令接口的第一层限速；应用层还会把失败次数持久化到 MySQL。
  limit_req_zone $binary_remote_addr zone=ops_login:10m rate=5r/m;

  # 供 WebSocket 使用
  map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
  }

  # 容器网络内访问 Next.js 服务
  upstream next_blog_upstream {
    server next-blog:3000;
    keepalive 64;
  }

  # 桶子倒计时 App 后端（countdown 项目独立部署）
  upstream countdown_api_upstream {
    server countdown-api:4000;
    keepalive 32;
  }

  # --- 80 跳转到 443 ---
  server {
    listen 80;
    server_name yoursigh.top www.yoursigh.top;
    return 301 https://$host$request_uri;
  }

  # --- 443 上的反向代理 ---
  server {
    listen 443 ssl http2;
    server_name yoursigh.top www.yoursigh.top;

    # 证书
    ssl_certificate     /www/yoursigh.top/ssl/yoursigh.top.pem;
	ssl_certificate_key /www/yoursigh.top/ssl/yoursigh.top.key;


    # TLS 建议
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:10m;

    client_max_body_size 20m;

    # 只有通过 Next 登录校验并返回 X-Accel-Redirect 后，Nginx 才会提供 APK。
    location ^~ /_protected/countdown-releases/ {
      internal;
      alias /var/www/countdown-releases/;
      default_type application/vnd.android.package-archive;
      add_header Cache-Control "private, no-store";
    }

    location = /api/ops/auth/login {
      limit_req zone=ops_login burst=3 nodelay;
      limit_req_status 429;

      proxy_pass http://next_blog_upstream;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_redirect off;
    }

    location = /api/apk/auth {
      limit_req zone=ops_login burst=3 nodelay;
      limit_req_status 429;

      proxy_pass http://next_blog_upstream;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_redirect off;
    }

    # countdown-api（Nginx 会去掉 /api/countdown 前缀）
    # 例：/api/countdown/health        -> countdown-api:4000/health
    #     /api/countdown/v1/events    -> countdown-api:4000/v1/events
    #     /api/countdown/ws           -> countdown-api:4000/ws
    location /api/countdown/ {
      proxy_pass http://countdown_api_upstream/;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
      proxy_redirect off;
      proxy_read_timeout 3600s;
      proxy_send_timeout 3600s;
    }

    # Next.js 静态资源：强缓存
    location ~* ^/_next/static/ {
      proxy_pass http://next_blog_upstream;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
      proxy_redirect off;

      add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # 其余请求：转发到 Next.js
    location / {
      proxy_pass http://next_blog_upstream;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
      proxy_redirect off;
    }
  }
}
